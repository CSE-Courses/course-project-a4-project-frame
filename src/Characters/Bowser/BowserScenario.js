import React from 'react'
import "../Character.css";

export default function BowserScenario() {
    return (
        <div>
            <h1>Pending (Bowser) - This is where the scenario components
                will be rendered out for each individual
                character
            </h1>
        </div>
    )
}

